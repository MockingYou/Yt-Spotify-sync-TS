import React from 'react';
import './LoadingRadial.css'; // Create a CSS file (LoadingRadial.css) and define the keyframes there

const LoadingRadial = () => {
  return (
      <span className="loader"></span>
  );
};

export default LoadingRadial;
