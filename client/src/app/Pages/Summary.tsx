import { Fragment, useEffect } from "react";
import { getSongs } from "../HelperFunctions/helperFunction";


 const Summary = ({}) => {
	useEffect(() => {
		// getSongs(source.name, playlistLink, setSourceLinkPlaylist, setLinkItem)
	}, [])
	return (
		<Fragment>
			<h1>CE VREI BA CACATULE?</h1>
		</Fragment>
	)
 }

 export default Summary;